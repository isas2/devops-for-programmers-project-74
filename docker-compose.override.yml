---

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    extends:
      file: docker-compose.yml
      service: app
    command: make dev
    ports:
      - "0.0.0.0:8080:8080"
    volumes:
      - ./app:/app
